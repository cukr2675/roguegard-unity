using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Roguegard.Device
{
    public delegate void ModelsMenuAction(IModelsMenuRoot root, RogueObj self, RogueObj user, in RogueMethodArgument arg);
}
